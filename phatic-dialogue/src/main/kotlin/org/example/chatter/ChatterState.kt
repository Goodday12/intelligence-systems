package org.example.chatter

enum class ChatterState( val stateName: String) {
    GREET_STATE("greet"), CHOOSE_STATE("choose"), BEER_STATE("beer"), WINE_STATE("wine"), WHISKEY_STATE("whiskey"), VODKA_STATE(
        "vodka"
    );


}